# go-tools-file-prefix

Rename files in a folder keeping only prefix

### Build

GOOS=darwin GOARCH=arm64 go build -o build/go-tools-file-prefix

GOOS=windows GOARCH=amd64 go build -o build/go-tools-file-prefix.exe
